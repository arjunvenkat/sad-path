<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>You've been invited to enroll in <%= @course.name %></h1>
    <% @user = User.new %>
    <%= render partial: '/users/form' %>
  </div>
</div>
