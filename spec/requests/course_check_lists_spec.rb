require 'spec_helper'

describe "CourseCheckLists" do
  describe "GET /course_check_lists" do
    it "works! (now write some real specs)" do
      get course_check_lists_path
      expect(response.status).to be(200)
    end
  end
end
