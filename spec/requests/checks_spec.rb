require 'spec_helper'

describe "Checks" do
  describe "GET /checks" do
    it "works! (now write some real specs)" do
      get checks_path
      expect(response.status).to be(200)
    end
  end
end
