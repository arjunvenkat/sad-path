require 'spec_helper'

describe "RoadblockChecks" do
  describe "GET /roadblock_checks" do
    it "works! (now write some real specs)" do
      get roadblock_checks_path
      expect(response.status).to be(200)
    end
  end
end
